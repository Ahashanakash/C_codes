// write a programme that shows absolute value of a number.

#include <stdio.h>

int main()
{
    int n, result;
    scanf("%d", &n);
    result = abs(n);
    printf("absolute value is = %d\n", result);

    return 0;
}