//write a programme that will convert an ASCII value to ASCII character.

#include<stdio.h>
int main (){
    int n;
    printf("Enter an ASCII value = ");
    scanf("%d",&n);

    printf("ASCII character is = %c",n);

    return 0;
}