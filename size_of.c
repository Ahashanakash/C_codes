//write a programme that will convert an ASCII character to ASCII value

#include<stdio.h>
int main (){
    int c;
    printf("Enter an ASCII character = ");
    scanf("%c",&c);
    printf("ASCII value is = %d",c);

    return 0;
}
